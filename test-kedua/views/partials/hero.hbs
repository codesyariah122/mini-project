<div class="p-5 mb-4 bg-light rounded-3">
	<div class="container-fluid py-5">
		<div class="row justify-content-center">
			<div v-for="hero in heros" class="col-md-6">
				<h1 class="display-5 fw-bold">\{{hero.title}}</h1>
				<p class="col-md-8 fs-4">\{{hero.desc}}.</p>

				{{!-- <pre>
					\{{coordinates}}
				</pre>

				<pre>
					{{config.apiKey}}
				</pre> --}}

				<button @click="dailyWeather(coordinates)" class="btn btn-primary rounded-pill">
					
					<div v-if="loading_daily">
						<span class="spinner-border spinner-border-sm" role="status" aria-hidden="true"></span>
						Loading...	
					</div>
					<div v-else>
						Lihat Cuaca \{{city}}
					</div>
					
				</button>
				{{!-- <div class="sharethis-inline-reaction-buttons mt-3 mb-3"></div> --}}
			</div>

		</div>

	</div>
</div>

